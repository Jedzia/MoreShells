#include standard
if not local, call horst ! ... ;
#extern horst
#define stresstest ;
#include "linkerscript.raW"



pfi = malloc(3);

console = 0;
while(0)
	stresstest(horst);
printf(runtime was %s), dllhell+18.2;
